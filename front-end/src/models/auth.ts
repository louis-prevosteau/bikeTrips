export interface Register {
    firstName: string;
    lastName: string;
    usename: string;
    email: string;
    password: string;
};

export interface Login {
    email: string;
    password: string;
};